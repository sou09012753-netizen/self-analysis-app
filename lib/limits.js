// コーチ1人あたりのクライアント登録上限。
// null = 無制限（上限チェックもUIのカウンタ表示も行わない）
//
// 再び上限を設ける場合はここに数値を入れれば、API・コーチ画面の両方に効く。
// コーチ単位で可変にする場合は coaches テーブルに max_clients 列を足し、
// この定数はフォールバック値として使う。
export const MAX_CLIENTS_PER_COACH = null;
